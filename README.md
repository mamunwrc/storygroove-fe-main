# Dev Setup

## .env

Create a `.env` file, and populate it with a copy from production.

Replace the following:
```
REACT_APP_BASE_URL="http://localhost:8086"
REACT_APP_BASE_API_URL="http://localhost:8086/"
```

## Running

You can run the backend simply by running `npm start`