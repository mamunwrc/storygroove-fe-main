import { getUserID } from "../service";
import { userTrack } from "./user";

export const getUserHistory = async (params) => {
  // const { type, socialMedium, imageUrl } = params;
  // const user = getUserID();
  // const body = {
  //   user,
  //   imageUrl,
  //   ...(type && { type }),
  //   ...(socialMedium && { socialMedium }),
  // };
  // const response = await userTrack(body);
  // return response.data;
};
